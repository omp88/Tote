package by.epam.tote.pool;


 enum DBConst {
	
	/** The db url. */
	DB_URL,
    
    /** The db username. */
    DB_USERNAME,
    
    /** The db password. */
    DB_PASSWORD,
    
    /** The db character encoding. */
    DB_CHARACTER_ENCODING,
    
    /** The db use ssl. */
    DB_USE_SSL,
    
    /** The db min pool size. */
    DB_MIN_POOL_SIZE,
    
    /** The db max pool size. */
    DB_MAX_POOL_SIZE

}
